<?php
 namespace WPAICG\Admin\Ajax\AIForms; use WP_Error; use WPAICG\AIForms\Admin\AIPKit_AI_Form_Ajax_Handler; if (!defined('ABSPATH')) { exit; } function do_ajax_import_forms_logic(AIPKit_AI_Form_Ajax_Handler $handler_instance): void { $form_storage = $handler_instance->get_form_storage(); if (!$form_storage) { $handler_instance->send_wp_error(new WP_Error('storage_missing_import', __('Form storage component is not available.', 'gpt3-ai-content-generator')), 500); return; } $forms_json = isset($_POST['forms_json']) ? wp_kses_post(wp_unslash($_POST['forms_json'])) : '{}'; if (empty($forms_json)) { $handler_instance->send_wp_error(new WP_Error('no_data_import', __('No form data received for import.', 'gpt3-ai-content-generator')), 400); return; } $forms_to_import = json_decode($forms_json, true); if (json_last_error() !== JSON_ERROR_NONE || !is_array($forms_to_import)) { $handler_instance->send_wp_error(new WP_Error('invalid_json_import', __('Invalid JSON data received for import.', 'gpt3-ai-content-generator')), 400); return; } $imported_count = 0; $failed_count = 0; $errors = []; $imported_records = []; foreach ($forms_to_import as $form_data) { $title = isset($form_data['title']) ? sanitize_text_field($form_data['title']) : 'Imported Form'; $settings = $form_data; unset($settings['title'], $settings['id'], $settings['status']); if (isset($settings['structure']) && is_array($settings['structure'])) { $settings['form_structure'] = wp_json_encode($settings['structure'], JSON_UNESCAPED_UNICODE | JSON_UNESCAPED_SLASHES); unset($settings['structure']); } $settings = apply_filters('aipkit_ai_forms_import_form_settings_before_create', $settings, $form_data, $forms_to_import, $form_storage); $new_title = $title . ' (Imported)'; $result = $form_storage->create_form($new_title, $settings); if (is_wp_error($result)) { $failed_count++; $errors[] = sprintf(__('Failed to import form "%1$s": %2$s', 'gpt3-ai-content-generator'), esc_html($title), $result->get_error_message()); } else { $imported_count++; $imported_records[] = [ 'new_id' => (int) $result, 'source' => $form_data, 'title' => $title, ]; } } $message = sprintf(_n('%d form was imported successfully.', '%d forms were imported successfully.', $imported_count, 'gpt3-ai-content-generator'), $imported_count); if ($failed_count > 0) { $message .= ' ' . sprintf(_n('%d form failed to import.','%d forms failed to import.',$failed_count,'gpt3-ai-content-generator'),$failed_count); } $extra_messages = apply_filters('aipkit_ai_forms_import_result_messages', [], $imported_records, $forms_to_import, $form_storage); if (is_array($extra_messages) && !empty($extra_messages)) { $message .= ' ' . implode(' ', array_map('sanitize_text_field', $extra_messages)); } wp_send_json_success(['message' => $message, 'imported_count' => $imported_count, 'failed_count' => $failed_count]); } 