<?php
 namespace WPAICG\AutoGPT\Ajax\Actions\SaveTask; use WPAICG\AutoGPT\Cron\AIPKit_Automated_Task_Scheduler; use WPAICG\AutoGPT\Cron\AIPKit_Automated_Task_Content_Queuer; use WPAICG\AutoGPT\Cron\EventProcessor\Trigger; if (!defined('ABSPATH')) { exit; } if (file_exists(WPAICG_PLUGIN_DIR . 'classes/autogpt/cron/event-processor/trigger/trigger-content-enhancement-task.php')) { require_once WPAICG_PLUGIN_DIR . 'classes/autogpt/cron/event-processor/trigger/trigger-content-enhancement-task.php'; } function finalize_task_save_logic(int $task_id, array $task_config, string $task_status, bool $is_new_task): void { if (class_exists(AIPKit_Automated_Task_Scheduler::class)) { $frequency = $task_config['task_frequency'] ?? ($task_config['indexing_frequency'] ?? 'daily'); AIPKit_Automated_Task_Scheduler::schedule_task_event($task_id, $frequency, $task_status); } if ( $is_new_task && $task_status === 'active' && ($task_config['task_type'] ?? '') === 'content_indexing' && ($task_config['index_existing_now_flag'] ?? '0') === '1' && class_exists(AIPKit_Automated_Task_Content_Queuer::class) ) { AIPKit_Automated_Task_Content_Queuer::maybe_queue_initial_indexing_content($task_id, $task_config); } elseif ( $is_new_task && $task_status === 'active' && ($task_config['task_type'] ?? '') === 'enhance_existing_content' && ($task_config['enhance_existing_now_flag'] ?? '0') === '1' && function_exists('\WPAICG\AutoGPT\Cron\EventProcessor\Trigger\trigger_content_enhancement_task_logic') ) { Trigger\trigger_content_enhancement_task_logic($task_id, $task_config, null); global $wpdb; $tasks_table_name = $wpdb->prefix . 'aipkit_automated_tasks'; $task_config['enhance_existing_now_flag'] = '0'; $wpdb->update( $tasks_table_name, ['task_config' => wp_json_encode($task_config)], ['id' => $task_id], ['%s'], ['%d'] ); } }