<?php
 namespace WPAICG\Speech; use WP_Error; if (!defined('ABSPATH')) { exit; } class AIPKit_TTS_Google_Provider_Strategy extends AIPKit_TTS_Base_Provider_Strategy { public function generate_speech(string $text, array $api_params, array $options): string|WP_Error { $api_key = $api_params['api_key'] ?? null; $voice_id = $options['voice'] ?? null; $output_format = $options['format'] ?? 'mp3'; if (empty($api_key)) { return new WP_Error('google_tts_missing_key', __('Google API Key is required for speech generation.', 'gpt3-ai-content-generator')); } if (empty($voice_id)) { return new WP_Error('google_tts_missing_voice', __('Google Voice ID is required for speech generation.', 'gpt3-ai-content-generator')); } if (empty($text)) { return new WP_Error('google_tts_empty_text', __('Text cannot be empty for speech generation.', 'gpt3-ai-content-generator')); } $audio_encoding = match (strtolower($output_format)) { 'mp3' => 'MP3', 'wav' => 'LINEAR16', 'ogg_opus' => 'OGG_OPUS', default => 'MP3', }; $language_code = substr($voice_id, 0, strpos($voice_id, '-', strpos($voice_id, '-') + 1) ?: 5); if (empty($language_code)) { $language_code = 'en-US'; } $url = 'https://texttospeech.googleapis.com/v1/text:synthesize?key=' . urlencode($api_key); $request_body = [ 'input' => ['text' => $text], 'voice' => [ 'languageCode' => $language_code, 'name' => $voice_id, ], 'audioConfig' => [ 'audioEncoding' => $audio_encoding, ], ]; $request_args = $this->get_request_options('generate_speech'); $request_args['method'] = 'POST'; $request_args['headers'] = $this->get_api_headers($api_key, 'generate_speech'); $request_args['body'] = wp_json_encode($request_body); $response = wp_remote_post($url, $request_args); if (is_wp_error($response)) { return new WP_Error('google_tts_http_error', __('HTTP error during speech generation.', 'gpt3-ai-content-generator'), ['status' => 503]); } $status_code = wp_remote_retrieve_response_code($response); $body = wp_remote_retrieve_body($response); if ($status_code !== 200) { $error_msg = $this->parse_error_response($body, $status_code, 'Google TTS Speech'); return new WP_Error('google_tts_api_error', sprintf(__('Google Speech API Error (%1$d): %2$s', 'gpt3-ai-content-generator'), $status_code, $error_msg), ['status' => $status_code]); } $decoded_response = $this->decode_json($body, 'Google TTS Speech'); if (is_wp_error($decoded_response)) { return new WP_Error($decoded_response->get_error_code(), $decoded_response->get_error_message(), ['status' => 500]); } if (empty($decoded_response['audioContent'])) { return new WP_Error('google_tts_no_audio', __('Google API returned success but no audio data.', 'gpt3-ai-content-generator'), ['status' => 500]); } $base64_audio_data = $decoded_response['audioContent']; return $base64_audio_data; } public function get_voices(array $api_params): array|WP_Error { $api_key = $api_params['api_key'] ?? null; if (empty($api_key)) { return new WP_Error('google_tts_missing_key', __('Google API Key is required to fetch voices.', 'gpt3-ai-content-generator')); } $url = 'https://texttospeech.googleapis.com/v1/voices?key=' . urlencode($api_key); $request_args = $this->get_request_options('voices'); $request_args['method'] = 'GET'; $response = wp_remote_get($url, $request_args); if (is_wp_error($response)) { return new WP_Error('google_tts_http_error', __('HTTP error fetching Google voices.', 'gpt3-ai-content-generator'), ['status' => 503]); } $status_code = wp_remote_retrieve_response_code($response); $body = wp_remote_retrieve_body($response); if ($status_code !== 200) { $error_msg = $this->parse_error_response($body, $status_code, 'Google TTS Voices'); return new WP_Error('google_tts_api_error', sprintf(__('Google Voices API Error (%1$d): %2$s', 'gpt3-ai-content-generator'), $status_code, $error_msg), ['status' => $status_code]); } $decoded = $this->decode_json($body, 'Google TTS Voices'); if (is_wp_error($decoded)) { return new WP_Error($decoded->get_error_code(), $decoded->get_error_message(), ['status' => 500]); } $voices_raw = $decoded['voices'] ?? []; $formatted_voices = []; if (is_array($voices_raw)) { foreach ($voices_raw as $voice) { if (!isset($voice['name']) || !isset($voice['languageCodes']) || !isset($voice['ssmlGender'])) continue; $language_code = $voice['languageCodes'][0] ?? 'Unknown'; $gender = strtolower($voice['ssmlGender']); $display_name = sprintf('%s (%s)', $voice['name'], $gender); $formatted_voices[] = [ 'id' => $voice['name'], 'name' => $display_name, 'languageCodes' => $voice['languageCodes'], 'gender' => $gender, ]; } usort($formatted_voices, fn($a, $b) => strcasecmp($a['name'], $b['name'])); } return $formatted_voices; } public function get_supported_formats(): array { return ['mp3', 'wav', 'ogg_opus']; } } 