<?php
namespace WPAICG\Vector\Providers\Pinecone\Methods; use WPAICG\Vector\Providers\AIPKit_Vector_Pinecone_Strategy; use WP_Error; if (!defined('ABSPATH')) { exit; } function _request_logic(AIPKit_Vector_Pinecone_Strategy $strategyInstance, string $method, string $path, array $body = [], ?string $index_host_url = null): array|WP_Error { if (!$strategyInstance->get_is_connected_status() && empty($index_host_url) && $path !== '/indexes?limit=1' ) { return new WP_Error('not_connected_pinecone', __('Not connected to Pinecone. Call connect() first or provide index host URL.', 'gpt3-ai-content-generator')); } $url = ($index_host_url ? rtrim($index_host_url, '/') : $strategyInstance->get_base_api_url()) . $path; $api_key = $strategyInstance->get_api_key(); $headers = [ 'Api-Key' => $api_key, 'Accept' => 'application/json', 'Content-Type' => 'application/json', ]; $request_args = [ 'method' => strtoupper($method), 'headers' => $headers, 'timeout' => 60, ]; if (!empty($body) && in_array(strtoupper($method), ['POST', 'PATCH'], true)) { $request_args['body'] = wp_json_encode($body); } $response = wp_remote_request($url, $request_args); if (is_wp_error($response)) { return $response; } $status_code = wp_remote_retrieve_response_code($response); $response_body = wp_remote_retrieve_body($response); if (strtoupper($method) === 'DELETE' && in_array($status_code, [200, 202, 204], true)) { return ['deleted' => true]; } if (strtoupper($method) === 'PATCH' && $status_code === 202) { return ['status' => 'accepted']; } $decoded_response = $strategyInstance->decode_json($response_body, 'Pinecone Vector Store'); if ($status_code >= 400) { $error_msg = $strategyInstance->parse_error_response($decoded_response ?: $response_body, $status_code, 'Pinecone Vector Store'); return new WP_Error('pinecone_api_error', sprintf(__('Pinecone API Error (%1$d): %2$s', 'gpt3-ai-content-generator'), $status_code, $error_msg)); } if (is_wp_error($decoded_response)) { return $decoded_response; } return $decoded_response; } function connect_logic(AIPKit_Vector_Pinecone_Strategy $strategyInstance, array $config): bool|WP_Error { if (empty($config['api_key'])) { return new WP_Error('missing_api_key_pinecone', __('Pinecone API Key is required.', 'gpt3-ai-content-generator')); } $strategyInstance->set_api_key($config['api_key']); $strategyInstance->set_is_connected_status(true); $test_list_response = $strategyInstance->list_indexes(1); if (is_wp_error($test_list_response)) { $strategyInstance->set_is_connected_status(false); return $test_list_response; } return true; } function create_index_if_not_exists_logic(AIPKit_Vector_Pinecone_Strategy $strategyInstance, string $index_name, array $index_config): array|WP_Error { $list_response = list_indexes_logic($strategyInstance); if (!is_wp_error($list_response) && is_array($list_response)) { foreach ($list_response as $existing_index) { if (isset($existing_index['name']) && $existing_index['name'] === $index_name) { return describe_index_logic($strategyInstance, $index_name); } } } $path = '/indexes'; $body = [ 'name' => $index_name, 'dimension' => absint($index_config['dimension'] ?? 1536), 'metric' => strtolower($index_config['metric'] ?? 'cosine'), 'spec' => $index_config['spec'] ?? [ 'serverless' => [ 'cloud' => $index_config['cloud'] ?? 'aws', 'region' => $index_config['region'] ?? 'us-east-1' ] ] ]; if (isset($index_config['deletion_protection'])) $body['deletion_protection'] = $index_config['deletion_protection']; $response = _request_logic($strategyInstance, 'POST', $path, $body); if (is_wp_error($response)) { return $response; } if (is_array($response) && isset($response['name'])) { sleep(15); return describe_index_logic($strategyInstance, $index_name); } return new WP_Error('pinecone_create_unknown_error', __('Unknown error creating Pinecone index.', 'gpt3-ai-content-generator')); } function delete_index_logic(AIPKit_Vector_Pinecone_Strategy $strategyInstance, string $index_name): bool|WP_Error { $path = '/indexes/' . urlencode($index_name); $response = _request_logic($strategyInstance, 'DELETE', $path); if (is_wp_error($response)) { return $response; } return isset($response['deleted']) && $response['deleted'] === true; } function delete_vectors_logic(AIPKit_Vector_Pinecone_Strategy $strategyInstance, string $index_name, array $vector_ids): bool|WP_Error { $index_description = get_index_overview_logic($strategyInstance, $index_name); if (is_wp_error($index_description)) return $index_description; $host = $index_description['host'] ?? null; if (empty($host)) return new WP_Error('missing_host_pinecone_delete_vectors', __('Index host not found for delete vectors.', 'gpt3-ai-content-generator')); $path = '/vectors/delete'; if (isset($vector_ids['ids']) && is_array($vector_ids['ids'])) { $body = ['ids' => array_values($vector_ids['ids'])]; } elseif (isset($vector_ids['filter']) && is_array($vector_ids['filter'])) { $body = ['filter' => $vector_ids['filter']]; } elseif (!empty($vector_ids['deleteAll'])) { $body = ['deleteAll' => true]; } else { $body = ['ids' => array_values($vector_ids)]; } if (isset($vector_ids['namespace']) && is_string($vector_ids['namespace']) && $vector_ids['namespace'] !== '') { $body['namespace'] = $vector_ids['namespace']; } $response = _request_logic($strategyInstance, 'POST', $path, $body, 'https://' . $host); if (is_wp_error($response)) return $response; return empty($response); } function get_index_overview_logic(AIPKit_Vector_Pinecone_Strategy $strategyInstance, string $index_name): array|WP_Error { $cache_key = 'aipkit_pinecone_index_overview_' . md5($index_name); $cache_group = 'aipkit_pinecone'; static $request_cache = []; if (isset($request_cache[$cache_key])) { return $request_cache[$cache_key]; } $cached = wp_cache_get($cache_key, $cache_group); if (false !== $cached) { $request_cache[$cache_key] = $cached; return $cached; } $transient = get_transient($cache_key); if (false !== $transient) { wp_cache_set($cache_key, $transient, $cache_group, HOUR_IN_SECONDS); $request_cache[$cache_key] = $transient; return $transient; } $path = '/indexes/' . urlencode($index_name); $description = _request_logic($strategyInstance, 'GET', $path); if (!is_wp_error($description)) { $ttl = (int) apply_filters('aipkit_pinecone_index_overview_cache_ttl', HOUR_IN_SECONDS, $index_name, $description); if ($ttl > 0) { set_transient($cache_key, $description, $ttl); wp_cache_set($cache_key, $description, $cache_group, $ttl); } } $request_cache[$cache_key] = $description; return $description; } function describe_index_logic(AIPKit_Vector_Pinecone_Strategy $strategyInstance, string $index_name): array|WP_Error { $description = get_index_overview_logic($strategyInstance, $index_name); if (is_wp_error($description)) { return $description; } $host = $description['host'] ?? null; if (empty($host)) { $description['total_vector_count'] = 'No Host'; return $description; } $stats_response = _request_logic($strategyInstance, 'POST', '/describe_index_stats', [], 'https://' . $host); if (is_wp_error($stats_response)) { $description['total_vector_count'] = 'Error'; } else { $description['total_vector_count'] = $stats_response['totalVectorCount'] ?? $stats_response['total_vector_count'] ?? 0; if (isset($stats_response['namespaces'])) { $description['namespaces'] = $stats_response['namespaces']; } } return $description; } function list_indexes_logic(AIPKit_Vector_Pinecone_Strategy $strategyInstance, ?int $limit = null, ?string $order = null, ?string $after = null, ?string $before = null): array|WP_Error { $path = '/indexes'; $list_response = _request_logic($strategyInstance, 'GET', $path); if (is_wp_error($list_response)) { return $list_response; } $indexes_data = $list_response['indexes'] ?? []; if (empty($indexes_data) && is_array($list_response)) { if (isset($list_response['collections'])) { $indexes_data = $list_response['collections']; } else { $indexes_data = $list_response; } } $formatted_indexes = []; if (is_array($indexes_data)) { foreach ($indexes_data as $index_obj_from_list) { $index_name = is_string($index_obj_from_list) ? $index_obj_from_list : ($index_obj_from_list['name'] ?? null); if (!$index_name) { continue; } $formatted_indexes[] = [ 'id' => $index_name, 'name' => $index_name, 'dimension' => $index_obj_from_list['dimension'] ?? null, 'metric' => $index_obj_from_list['metric'] ?? null, 'host' => $index_obj_from_list['host'] ?? null, 'status' => $index_obj_from_list['status'] ?? null, 'spec' => $index_obj_from_list['spec'] ?? null, ]; } } return $formatted_indexes; } function query_vectors_logic(AIPKit_Vector_Pinecone_Strategy $strategyInstance, string $index_name, array $query_vector_param, int $top_k, array $filter = []): array|WP_Error { $index_description = get_index_overview_logic($strategyInstance, $index_name); if (is_wp_error($index_description)) return $index_description; $host = $index_description['host'] ?? null; if (empty($host)) return new WP_Error('missing_host_pinecone_query', __('Index host not found for query operation.', 'gpt3-ai-content-generator')); if (!isset($query_vector_param['vector']) || !is_array($query_vector_param['vector'])) { return new WP_Error('invalid_query_vector_structure', __('Query vector must contain a "vector" key with an array of embeddings.', 'gpt3-ai-content-generator')); } $body = [ 'vector' => $query_vector_param['vector'], 'topK' => $top_k, 'includeMetadata' => true, 'includeValues' => false ]; if (!empty($filter)) { $body['filter'] = $filter; } if (isset($query_vector_param['namespace']) && !empty($query_vector_param['namespace'])) { $body['namespace'] = $query_vector_param['namespace']; } $response = _request_logic($strategyInstance, 'POST', '/query', $body, 'https://' . $host); if (is_wp_error($response)) return $response; $matches = $response['matches'] ?? []; $results = []; foreach($matches as $match) { $results[] = [ 'id' => $match['id'] ?? null, 'score' => $match['score'] ?? null, 'metadata' => $match['metadata'] ?? [], ]; } return $results; } function upload_file_for_vector_store_logic(AIPKit_Vector_Pinecone_Strategy $strategyInstance, string $file_path, string $original_filename, string $purpose = 'user_data'): array|WP_Error { return new WP_Error('not_applicable_pinecone_file_upload', __('Direct file upload is not applicable for Pinecone. Generate embeddings and upsert vectors.', 'gpt3-ai-content-generator')); } function upsert_vectors_logic(AIPKit_Vector_Pinecone_Strategy $strategyInstance, string $index_name, array $vectors_data): array|WP_Error { $index_description = get_index_overview_logic($strategyInstance, $index_name); if (is_wp_error($index_description)) return $index_description; $host = $index_description['host'] ?? null; if (empty($host)) return new WP_Error('missing_host_pinecone_upsert', __('Index host not found for upsert operation.', 'gpt3-ai-content-generator')); $path = '/vectors/upsert'; $vectors_list = $vectors_data['vectors'] ?? $vectors_data; if (!is_array($vectors_list)) { return new WP_Error('pinecone_upsert_invalid_vectors', __('Invalid vectors payload for upsert.', 'gpt3-ai-content-generator')); } $expected_dim = isset($index_description['dimension']) ? (int) $index_description['dimension'] : null; if ($expected_dim && $expected_dim > 0) { foreach ($vectors_list as $vec) { $values = $vec['values'] ?? $vec['vector'] ?? null; if (is_array($values) && count($values) !== $expected_dim) { return new WP_Error('vector_dimension_mismatch', sprintf(__('Vector dimension mismatch. Expected %1$d, got %2$d.', 'gpt3-ai-content-generator'), $expected_dim, count($values))); } } } else { $first_len = null; foreach ($vectors_list as $vec) { $values = $vec['values'] ?? $vec['vector'] ?? null; if (!is_array($values)) continue; $len = count($values); if ($first_len === null) { $first_len = $len; } if ($first_len !== $len) { return new WP_Error('vector_dimension_inconsistent', __('Vectors have inconsistent dimensions in the upsert payload.', 'gpt3-ai-content-generator')); } } } $namespace = $vectors_data['namespace'] ?? null; $batch_size = apply_filters('aipkit_pinecone_upsert_batch_size', 100, $index_name); $all_results = []; $total = count($vectors_list); for ($i = 0; $i < $total; $i += $batch_size) { $chunk = array_slice($vectors_list, $i, $batch_size); $body = ['vectors' => $chunk]; if ($namespace) { $body['namespace'] = $namespace; } $resp = _request_logic($strategyInstance, 'POST', $path, $body, 'https://' . $host); if (is_wp_error($resp)) return $resp; $all_results[] = $resp; } $last = end($all_results); if (is_array($last)) { $last['aipkit_batches'] = count($all_results); } return $last; } 