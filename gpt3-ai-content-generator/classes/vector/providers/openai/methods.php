<?php
namespace WPAICG\Vector\Providers\OpenAI\Methods; use WPAICG\Core\AIPKit_HTTP_Request; use WPAICG\Vector\Providers\AIPKit_Vector_OpenAI_Strategy; use WP_Error; use WPAICG\Core\Providers\OpenAI\OpenAIUrlBuilder; use CURLFile; if (!defined('ABSPATH')) { exit; } function _request_logic(AIPKit_Vector_OpenAI_Strategy $strategyInstance, string $method, string $url, array $body = [], bool $is_file_upload = false): array|WP_Error { $api_key = $strategyInstance->get_api_key(); if (empty($api_key)) { return new WP_Error('openai_vector_missing_key', __('OpenAI API Key is not configured.', 'gpt3-ai-content-generator')); } $headers = [ 'Authorization' => 'Bearer ' . $api_key, 'OpenAI-Beta' => 'assistants=v2', ]; if (!$is_file_upload) { $headers['Content-Type'] = 'application/json'; } $request_args = [ 'method' => strtoupper($method), 'headers' => $headers, 'timeout' => 120, ]; if (!$is_file_upload && !empty($body) && ($method === 'POST' || $method === 'PUT' || $method === 'PATCH')) { $request_args['body'] = wp_json_encode($body); } elseif ($is_file_upload && !empty($body)) { $request_args['body'] = $body; } $response_body = null; $status_code = null; if ($is_file_upload) { $ch = curl_init(); curl_setopt($ch, CURLOPT_URL, $url); curl_setopt($ch, CURLOPT_RETURNTRANSFER, true); curl_setopt($ch, CURLOPT_CUSTOMREQUEST, strtoupper($method)); curl_setopt($ch, CURLOPT_POSTFIELDS, $body); $curl_headers = []; foreach ($headers as $key => $value) { $curl_headers[] = "{$key}: {$value}"; } curl_setopt($ch, CURLOPT_HTTPHEADER, $curl_headers); curl_setopt($ch, CURLOPT_TIMEOUT, 120); $response_body = curl_exec($ch); $status_code = curl_getinfo($ch, CURLINFO_HTTP_CODE); $curl_error = curl_error($ch); $ch = null; if ($curl_error) { return new WP_Error('openai_vector_curl_error', 'cURL error during request: ' . $curl_error); } } else { $response = class_exists(AIPKit_HTTP_Request::class) ? AIPKit_HTTP_Request::request($url, $request_args, true) : wp_remote_request($url, $request_args); if (is_wp_error($response)) { return $response; } $status_code = wp_remote_retrieve_response_code($response); $response_body = wp_remote_retrieve_body($response); } $decoded_response = $strategyInstance->decode_json($response_body, 'OpenAI Vector Store'); if ($status_code >= 400) { $error_msg = $strategyInstance->parse_error_response($decoded_response ?: $response_body, $status_code, 'OpenAI Vector Store'); return new WP_Error('openai_vector_api_error', sprintf(__('OpenAI Vector API Error (%1$d): %2$s', 'gpt3-ai-content-generator'), $status_code, esc_html($error_msg))); } if (is_wp_error($decoded_response)) { return $decoded_response; } return $decoded_response; } function connect_logic(AIPKit_Vector_OpenAI_Strategy $strategyInstance, array $config): bool|WP_Error { if (empty($config['api_key'])) { return new WP_Error('missing_api_key', __('OpenAI API Key is required for connection.', 'gpt3-ai-content-generator')); } if (!$strategyInstance->get_is_connected_status()) { return new WP_Error('internal_error_connect_openai', 'Strategy instance not marked as connected before test call.'); } $test_list_response = \WPAICG\Vector\Providers\OpenAI\Methods\_request_logic( $strategyInstance, 'GET', \WPAICG\Core\Providers\OpenAI\OpenAIUrlBuilder::build('vector_stores', [ 'base_url' => $strategyInstance->get_base_url(), 'api_version' => $strategyInstance->get_api_version(), 'limit' => 1 ]) ); if (is_wp_error($test_list_response)) { return $test_list_response; } return true; } function create_index_if_not_exists_logic(AIPKit_Vector_OpenAI_Strategy $strategyInstance, string $index_name, array $index_config): array|WP_Error { if (!$strategyInstance->get_is_connected_status()) { return new WP_Error('not_connected', __('Not connected to OpenAI.', 'gpt3-ai-content-generator')); } $list_response = list_indexes_logic($strategyInstance, 100); if (!is_wp_error($list_response) && isset($list_response['data'])) { foreach ($list_response['data'] as $store) { if (isset($store['name']) && $store['name'] === $index_name) { return $store; } } } $url_params = [ 'base_url' => $strategyInstance->get_base_url(), 'api_version' => $strategyInstance->get_api_version() ]; $url = OpenAIUrlBuilder::build('vector_stores', $url_params); if (is_wp_error($url)) return $url; $body = ['name' => $index_name]; if (isset($index_config['file_ids'])) $body['file_ids'] = $index_config['file_ids']; if (isset($index_config['metadata'])) $body['metadata'] = $index_config['metadata']; if (isset($index_config['expires_after']) && is_array($index_config['expires_after'])) { $body['expires_after'] = $index_config['expires_after']; } if (isset($index_config['chunking_strategy'])) { $body['chunking_strategy'] = $index_config['chunking_strategy']; } elseif (!empty($index_config['file_ids']) && is_array($index_config['file_ids'])) { $configured_chunking_strategy = apply_filters( 'aipkit_openai_file_search_chunking_strategy', null, [ 'operation' => 'create_vector_store', 'index_name' => $index_name, 'file_ids' => $index_config['file_ids'], ] ); if (is_array($configured_chunking_strategy)) { $body['chunking_strategy'] = $configured_chunking_strategy; } } $response = _request_logic($strategyInstance, 'POST', $url, $body); if (is_wp_error($response)) return $response; return isset($response['id']) ? $response : new WP_Error('store_creation_malformed_response', __('Malformed response after store creation.', 'gpt3-ai-content-generator')); } function delete_index_logic(AIPKit_Vector_OpenAI_Strategy $strategyInstance, string $index_name): bool|WP_Error { if (!$strategyInstance->get_is_connected_status()) { return new WP_Error('not_connected', __('Not connected to OpenAI.', 'gpt3-ai-content-generator')); } $vector_store_id = $index_name; $url_params = [ 'base_url' => $strategyInstance->get_base_url(), 'api_version' => $strategyInstance->get_api_version(), 'vector_store_id' => $vector_store_id ]; $url = OpenAIUrlBuilder::build('vector_stores_id', $url_params); if (is_wp_error($url)) return $url; $response = _request_logic($strategyInstance, 'DELETE', $url); if (is_wp_error($response)) return $response; return isset($response['deleted']) && $response['deleted'] === true; } function delete_openai_file_object_logic(AIPKit_Vector_OpenAI_Strategy $strategyInstance, string $file_id): bool|WP_Error { if (!$strategyInstance->get_is_connected_status()) { return new WP_Error('not_connected', __('Not connected to OpenAI.', 'gpt3-ai-content-generator')); } if (empty($file_id)) { return new WP_Error('missing_file_id', __('File ID is required to delete the file object.', 'gpt3-ai-content-generator')); } $url_params = [ 'base_url' => $strategyInstance->get_base_url(), 'api_version' => $strategyInstance->get_api_version(), 'file_id' => $file_id ]; $url = OpenAIUrlBuilder::build('files_id', $url_params); if (is_wp_error($url)) { $version_segment = '/' . trim($strategyInstance->get_api_version(), '/'); $url = $strategyInstance->get_base_url() . $version_segment . '/files/' . urlencode($file_id); } $response = _request_logic($strategyInstance, 'DELETE', $url); if (is_wp_error($response)) return $response; if (isset($response['deleted']) && $response['deleted'] === true && isset($response['id']) && $response['id'] === $file_id) { return true; } else { return new WP_Error('file_object_delete_failed', __('OpenAI API did not confirm file object deletion.', 'gpt3-ai-content-generator')); } } function delete_vectors_logic(AIPKit_Vector_OpenAI_Strategy $strategyInstance, string $index_name, array $vector_ids): bool|WP_Error { if (!$strategyInstance->get_is_connected_status()) { return new WP_Error('not_connected', __('Not connected to OpenAI.', 'gpt3-ai-content-generator')); } if (empty($vector_ids)) return true; $vector_store_id = $index_name; $all_successful = true; foreach ($vector_ids as $file_id) { $url_params = [ 'base_url' => $strategyInstance->get_base_url(), 'api_version' => $strategyInstance->get_api_version(), 'vector_store_id' => $vector_store_id, 'file_id' => $file_id ]; $url = OpenAIUrlBuilder::build('vector_stores_id_files_id', $url_params); if (is_wp_error($url)) { $all_successful = false; continue; } $response = _request_logic($strategyInstance, 'DELETE', $url); if (is_wp_error($response)) { $all_successful = false; } elseif (!isset($response['deleted']) || $response['deleted'] !== true) { $all_successful = false; } } return $all_successful ? true : new WP_Error('partial_delete_failure', __('Some files could not be deleted from the vector store.', 'gpt3-ai-content-generator')); } function describe_index_logic(AIPKit_Vector_OpenAI_Strategy $strategyInstance, string $index_name): array|WP_Error { if (!$strategyInstance->get_is_connected_status()) { return new WP_Error('not_connected', __('Not connected to OpenAI.', 'gpt3-ai-content-generator')); } $vector_store_id = $index_name; $url_params = [ 'base_url' => $strategyInstance->get_base_url(), 'api_version' => $strategyInstance->get_api_version(), 'vector_store_id' => $vector_store_id ]; $url = OpenAIUrlBuilder::build('vector_stores_id', $url_params); if (is_wp_error($url)) return $url; return _request_logic($strategyInstance, 'GET', $url); } function get_mime_type_from_filename_logic(AIPKit_Vector_OpenAI_Strategy $strategyInstance, string $filename): string|WP_Error { $extension = strtolower(pathinfo($filename, PATHINFO_EXTENSION)); if (empty($extension)) { return new WP_Error('missing_extension', __('Filename has no extension, cannot determine MIME type.', 'gpt3-ai-content-generator')); } $mime_map = $strategyInstance::get_static_mime_type_map(); if (isset($mime_map[$extension])) { return $mime_map[$extension]; } return new WP_Error('unsupported_extension_for_mime', sprintf(__('File extension ".%s" is not explicitly mapped to a supported MIME type for OpenAI.', 'gpt3-ai-content-generator'), $extension)); } function list_indexes_logic(AIPKit_Vector_OpenAI_Strategy $strategyInstance, ?int $limit = 20, ?string $order = 'desc', ?string $after = null, ?string $before = null): array|WP_Error { if (!$strategyInstance->get_is_connected_status()) { return new WP_Error('not_connected', __('Not connected to OpenAI.', 'gpt3-ai-content-generator')); } if (!class_exists(\WPAICG\Core\Providers\OpenAI\OpenAIUrlBuilder::class)) { $url_builder_bootstrap = WPAICG_PLUGIN_DIR . 'classes/core/providers/openai/bootstrap-provider-strategy.php'; if (file_exists($url_builder_bootstrap)) { require_once $url_builder_bootstrap; } else { return new WP_Error('openai_url_builder_missing_logic', 'OpenAI URL builder component is not available.'); } } $url_params = [ 'base_url' => $strategyInstance->get_base_url(), 'api_version' => $strategyInstance->get_api_version(), 'limit' => $limit, 'order' => $order, 'after' => $after, 'before' => $before, ]; $url = \WPAICG\Core\Providers\OpenAI\OpenAIUrlBuilder::build('vector_stores', $url_params); if (is_wp_error($url)) return $url; $response = _request_logic($strategyInstance, 'GET', $url); if (is_wp_error($response)) return $response; return [ 'data' => $response['data'] ?? [], 'first_id' => $response['first_id'] ?? null, 'last_id' => $response['last_id'] ?? null, 'has_more' => $response['has_more'] ?? false, ]; } function list_vector_store_files_logic(AIPKit_Vector_OpenAI_Strategy $strategyInstance, string $vector_store_id, array $query_params = []): array|WP_Error { if (!$strategyInstance->get_is_connected_status()) { return new WP_Error('not_connected', __('Not connected to OpenAI.', 'gpt3-ai-content-generator')); } $url_params = [ 'base_url' => $strategyInstance->get_base_url(), 'api_version' => $strategyInstance->get_api_version(), 'vector_store_id' => $vector_store_id ]; $url = OpenAIUrlBuilder::build('vector_stores_id_files', $url_params); if (is_wp_error($url)) return $url; if (!empty($query_params)) { $url = add_query_arg(array_map('sanitize_text_field', $query_params), $url); } $response = _request_logic($strategyInstance, 'GET', $url); if (is_wp_error($response)) return $response; return $response['data'] ?? []; } function query_vectors_logic(AIPKit_Vector_OpenAI_Strategy $strategyInstance, string $index_name, array $query_vector, int $top_k, array $filter = []): array|WP_Error { if (!$strategyInstance->get_is_connected_status()) { return new WP_Error('not_connected', __('Not connected to OpenAI.', 'gpt3-ai-content-generator')); } $vector_store_id = $index_name; if (!isset($query_vector['query_text']) || !is_string($query_vector['query_text'])) { return new WP_Error('invalid_query_type', __('For OpenAI vector store search, query_vector must be an array containing a "query_text" string.', 'gpt3-ai-content-generator')); } $url_params = [ 'base_url' => $strategyInstance->get_base_url(), 'api_version' => $strategyInstance->get_api_version(), 'vector_store_id' => $vector_store_id ]; $url = OpenAIUrlBuilder::build('vector_stores_id_search', $url_params); if (is_wp_error($url)) return $url; $body = [ 'query' => $query_vector['query_text'], 'max_num_results' => max(1, min($top_k, 50)), ]; if (!empty($filter)) { $body['filters'] = $filter; } if (isset($query_vector['ranking_options'])) { $body['ranking_options'] = $query_vector['ranking_options']; } $response = _request_logic($strategyInstance, 'POST', $url, $body); if (is_wp_error($response)) return $response; $results = []; if (isset($response['data']) && is_array($response['data'])) { foreach ($response['data'] as $item) { $results[] = [ 'id' => $item['file_id'] ?? null, 'score' => $item['score'] ?? null, 'metadata' => $item['attributes'] ?? [], 'content' => $item['content'] ?? null, 'raw_item' => $item ]; } } return $results; } function retrieve_file_batch_logic(AIPKit_Vector_OpenAI_Strategy $strategyInstance, string $vector_store_id, string $batch_id): array|WP_Error { if (!$strategyInstance->get_is_connected_status()) { return new WP_Error('not_connected', __('Not connected to OpenAI.', 'gpt3-ai-content-generator')); } $url_params = [ 'base_url' => $strategyInstance->get_base_url(), 'api_version' => $strategyInstance->get_api_version(), 'vector_store_id' => $vector_store_id, 'batch_id' => $batch_id ]; $url = OpenAIUrlBuilder::build('vector_stores_id_file_batches_id', $url_params); if (is_wp_error($url)) return $url; return _request_logic($strategyInstance, 'GET', $url); } function upload_file_for_vector_store_logic(AIPKit_Vector_OpenAI_Strategy $strategyInstance, string $file_path, string $original_filename, string $purpose = 'assistants_file'): array|WP_Error { if (!$strategyInstance->get_is_connected_status()) { return new WP_Error('not_connected', __('Not connected to OpenAI.', 'gpt3-ai-content-generator')); } if (!file_exists($file_path) || !is_readable($file_path)) { return new WP_Error('file_not_readable', __('File not found or not readable at path: ', 'gpt3-ai-content-generator') . $file_path); } if (!class_exists('CURLFile')) { return new WP_Error('curlfile_missing', __('Server configuration error (CURLFile missing for file upload).', 'gpt3-ai-content-generator'), ['status' => 500]); } $url_builder_params = [ 'base_url' => $strategyInstance->get_base_url(), 'api_version' => $strategyInstance->get_api_version() ]; $url = OpenAIUrlBuilder::build('files', $url_builder_params); if (is_wp_error($url)) return $url; $mime_type = get_mime_type_from_filename_logic($strategyInstance, $original_filename); if (is_wp_error($mime_type)) { $mime_type = 'application/octet-stream'; } $cfile = new CURLFile($file_path, $mime_type, $original_filename); $data = ['purpose' => $purpose, 'file' => $cfile]; return _request_logic($strategyInstance, 'POST', $url, $data, true); } function upsert_vectors_logic(AIPKit_Vector_OpenAI_Strategy $strategyInstance, string $index_name, array $vectors): array|WP_Error { if (!$strategyInstance->get_is_connected_status()) { return new WP_Error('not_connected', __('Not connected to OpenAI.', 'gpt3-ai-content-generator')); } $vector_store_id = $index_name; $file_ids = $vectors['file_ids'] ?? null; if (empty($file_ids) || !is_array($file_ids)) { return new WP_Error('missing_file_ids', __('File IDs are required for upserting to OpenAI Vector Store.', 'gpt3-ai-content-generator')); } $url_params = [ 'base_url' => $strategyInstance->get_base_url(), 'api_version' => $strategyInstance->get_api_version(), 'vector_store_id' => $vector_store_id ]; $url = OpenAIUrlBuilder::build('vector_stores_id_file_batches', $url_params); if (is_wp_error($url)) return $url; $body = ['file_ids' => $file_ids]; if (isset($vectors['chunking_strategy'])) { $body['chunking_strategy'] = $vectors['chunking_strategy']; } else { $configured_chunking_strategy = apply_filters( 'aipkit_openai_file_search_chunking_strategy', null, [ 'operation' => 'file_batch', 'vector_store_id' => $vector_store_id, 'file_ids' => $file_ids, ] ); if (is_array($configured_chunking_strategy)) { $body['chunking_strategy'] = $configured_chunking_strategy; } } return _request_logic($strategyInstance, 'POST', $url, $body); } 