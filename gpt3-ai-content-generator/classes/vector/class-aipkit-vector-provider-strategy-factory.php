<?php
 namespace WPAICG\Vector; use WPAICG\AIPKit_Providers; use WP_Error; if (!defined('ABSPATH')) { exit; } class AIPKit_Vector_Provider_Strategy_Factory { private static $instances = []; public static function get_strategy(string $provider): AIPKit_Vector_Provider_Strategy_Interface|WP_Error { if (isset(self::$instances[$provider])) { return self::$instances[$provider]; } if ( class_exists(AIPKit_Providers::class) && !AIPKit_Providers::provider_supports_capability($provider, 'vector_stores') ) { return new WP_Error( 'vector_provider_not_supported', sprintf( __('Vector stores are not supported by %s in this integration.', 'gpt3-ai-content-generator'), esc_html($provider) ), ['status' => 501] ); } $strategy_path_base = __DIR__ . '/providers/'; $strategies_map = [ 'Pinecone' => 'pinecone/bootstrap.php', 'Qdrant' => 'qdrant/bootstrap.php', 'OpenAI' => 'openai/bootstrap.php', 'Chroma' => 'chroma/bootstrap.php', ]; if (!isset($strategies_map[$provider])) { return new WP_Error('unsupported_vector_provider', sprintf(__('Vector store provider "%s" is not supported.', 'gpt3-ai-content-generator'), esc_html($provider))); } $strategy_file = $strategy_path_base . $strategies_map[$provider]; $class_name = null; switch ($provider) { case 'Pinecone': $class_name = \WPAICG\Vector\Providers\AIPKit_Vector_Pinecone_Strategy::class; break; case 'Qdrant': $class_name = \WPAICG\Vector\Providers\AIPKit_Vector_Qdrant_Strategy::class; break; case 'OpenAI': $class_name = \WPAICG\Vector\Providers\AIPKit_Vector_OpenAI_Strategy::class; break; case 'Chroma': $class_name = \WPAICG\Vector\Providers\AIPKit_Vector_Chroma_Strategy::class; break; default: return new WP_Error('unsupported_vector_provider_strategy', sprintf(__('Vector store provider strategy "%s" is not supported.', 'gpt3-ai-content-generator'), esc_html($provider))); } $interface_path = __DIR__ . '/interface-aipkit-vector-provider-strategy.php'; $base_class_path = __DIR__ . '/class-aipkit-vector-base-provider-strategy.php'; if (file_exists($interface_path) && !interface_exists(AIPKit_Vector_Provider_Strategy_Interface::class)) { require_once $interface_path; } if (file_exists($base_class_path) && !class_exists(AIPKit_Vector_Base_Provider_Strategy::class)) { require_once $base_class_path; } if (!class_exists($class_name)) { if (file_exists($strategy_file)) { require_once $strategy_file; } else { return new WP_Error('vector_strategy_file_not_found', sprintf(__('Vector Strategy file not found for provider: %s', 'gpt3-ai-content-generator'), esc_html($provider))); } } if (class_exists($class_name)) { self::$instances[$provider] = new $class_name(); } else { return new WP_Error('vector_strategy_instantiation_failed', sprintf(__('Failed to load Vector Store strategy for provider: %s', 'gpt3-ai-content-generator'), esc_html($provider))); } return self::$instances[$provider]; } } 