<?php
 namespace WPAICG\Core\TokenManager\Cron; if (!defined('ABSPATH')) { exit; } function ScheduleTokenResetEventLogic(string $cronHook): void { if (!wp_next_scheduled($cronHook)) { wp_schedule_event(time(), 'daily', $cronHook); } }