<?php
 namespace WPAICG\Core\Providers\Google\Methods; if (!defined('ABSPATH')) { exit; } function _shared_format_logic(string $instructions, array $history, array $ai_params): array { $contents = []; foreach ($history as $msg) { if (isset($msg['role']) && $msg['role'] === 'system') { continue; } $role = ($msg['role'] === 'bot' || $msg['role'] === 'assistant') ? 'model' : 'user'; $content_parts = []; if (isset($msg['content']) && is_array($msg['content'])) { foreach ($msg['content'] as $part) { if (isset($part['type'])) { if ($part['type'] === 'text' || $part['type'] === 'input_text') { $content_parts[] = ['text' => $part['text'] ?? '']; } elseif (($part['type'] === 'image_url' || $part['type'] === 'input_image') && isset($part['image_url'])) { if (is_string($part['image_url']) && strpos($part['image_url'], 'data:') === 0) { list($type, $data) = explode(';', $part['image_url']); list(, $data) = explode(',', $data); $mime_type = substr($type, 5); $content_parts[] = ['inline_data' => ['mime_type' => $mime_type, 'data' => $data]]; } else if (is_array($part['image_url']) && isset($part['image_url']['url']) && strpos($part['image_url']['url'], 'data:') === 0){ list($type, $data) = explode(';', $part['image_url']['url']); list(, $data) = explode(',', $data); $mime_type = substr($type, 5); $content_parts[] = ['inline_data' => ['mime_type' => $mime_type, 'data' => $data]]; } } } } } elseif (isset($msg['content']) && is_string($msg['content'])) { $content_parts[] = ['text' => trim($msg['content'])]; } if (!empty($content_parts)) { if ($role === 'user' || $role === 'model') { $contents[] = ['role' => $role, 'parts' => $content_parts]; } } } if (!empty($ai_params['image_inputs']) && is_array($ai_params['image_inputs'])) { $image_parts = []; foreach ($ai_params['image_inputs'] as $image_input) { if (!is_array($image_input)) { continue; } $mime_type = isset($image_input['type']) ? sanitize_text_field((string) $image_input['type']) : ''; $base64_data = isset($image_input['base64']) ? trim((string) $image_input['base64']) : ''; if ($mime_type === '' || $base64_data === '') { continue; } $image_parts[] = [ 'inline_data' => [ 'mime_type' => $mime_type, 'data' => $base64_data, ], ]; } if (!empty($image_parts)) { $last_user_index = null; for ($i = count($contents) - 1; $i >= 0; $i--) { if (($contents[$i]['role'] ?? '') === 'user') { $last_user_index = $i; break; } } if ($last_user_index !== null) { $contents[$last_user_index]['parts'] = array_merge($contents[$last_user_index]['parts'], $image_parts); } else { $contents[] = ['role' => 'user', 'parts' => $image_parts]; } } } $cleaned_contents = []; $last_role = null; foreach ($contents as $msg) { if ($msg['role'] !== $last_role) { $cleaned_contents[] = $msg; $last_role = $msg['role']; } else { $last_index = count($cleaned_contents) - 1; if ($last_index >= 0 && $cleaned_contents[$last_index]['role'] === $msg['role']) { $cleaned_contents[$last_index]['parts'] = array_merge($cleaned_contents[$last_index]['parts'], $msg['parts']); } else { $cleaned_contents[] = $msg; $last_role = $msg['role']; } } } $body_data = ['contents' => $cleaned_contents]; if (!empty($instructions)) { $body_data['system_instruction'] = ['parts' => [['text' => $instructions]]]; } $generationConfig = []; $param_map = [ 'temperature' => 'temperature', 'max_completion_tokens' => 'maxOutputTokens', 'top_p' => 'topP', 'stop' => 'stopSequences', ]; foreach ($param_map as $aipkit_key => $api_key) { if (isset($ai_params[$aipkit_key])) { $value = $ai_params[$aipkit_key]; if ($api_key === 'temperature' || $api_key === 'topP') { $generationConfig[$api_key] = floatval($value); } elseif ($api_key === 'maxOutputTokens') { $generationConfig[$api_key] = absint($value); } elseif ($api_key === 'stopSequences' && !empty($value)) { $generationConfig[$api_key] = is_string($value) ? [$value] : (is_array($value) ? $value : null); if (empty($generationConfig[$api_key])) unset($generationConfig[$api_key]); } } } if (!empty($generationConfig)) $body_data['generationConfig'] = $generationConfig; if (isset($ai_params['safety_settings']) && is_array($ai_params['safety_settings'])) { $body_data['safetySettings'] = $ai_params['safety_settings']; } $google_search_grounding_active = $ai_params['frontend_google_search_grounding_active'] ?? false; $model_name_for_grounding = strtolower((string) ($ai_params['model_id_for_grounding'] ?? '')); if ($google_search_grounding_active) { $tools = []; if (strpos($model_name_for_grounding, 'gemini-1.5-flash') !== false) { $grounding_mode = $ai_params['google_grounding_mode'] ?? 'DEFAULT_MODE'; if ($grounding_mode === 'MODE_DYNAMIC') { $dynamic_threshold = $ai_params['google_grounding_dynamic_threshold'] ?? 0.3; $tools[] = [ 'google_search_retrieval' => [ 'dynamic_retrieval_config' => [ 'mode' => 'MODE_DYNAMIC', 'dynamic_threshold' => floatval($dynamic_threshold), ] ] ]; } else { $tools[] = ['google_search_retrieval' => new \stdClass()]; } } elseif (strpos($model_name_for_grounding, 'gemini') !== false) { $tools[] = ['google_search' => new \stdClass()]; } if (!empty($tools)) { $body_data['tools'] = $tools; } } return $body_data; } 