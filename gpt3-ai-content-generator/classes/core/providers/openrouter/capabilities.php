<?php
 namespace WPAICG\Core\Providers\OpenRouter\Methods; use WPAICG\Utils\AIPKit_Prompt_Sanitizer; if (!defined('ABSPATH')) { exit; } function get_capability_map_logic(): array { return [ 'chat' => true, 'stream' => true, 'tools' => true, 'web_search_plugin' => true, 'image_input' => true, 'image_output' => true, 'image_generation' => true, 'embeddings' => true, ]; } function normalize_capability_list_logic(mixed $value): array { if (!is_array($value)) { return []; } $normalized = []; foreach ($value as $entry) { if (!is_string($entry)) { continue; } $entry = strtolower(trim($entry)); if ($entry !== '') { $normalized[] = $entry; } } return array_values(array_unique($normalized)); } function sanitize_capabilities_payload_logic(mixed $capabilities): array { if (!is_array($capabilities)) { return []; } $allowed_keys = array_keys(get_capability_map_logic()); $sanitized = []; foreach ($allowed_keys as $key) { if (!array_key_exists($key, $capabilities)) { continue; } $sanitized[$key] = (bool) $capabilities[$key]; } return $sanitized; } function resolve_model_capabilities_from_metadata_logic(array $metadata): array { $resolved = get_capability_map_logic(); $resolved['image_output'] = false; $resolved['image_generation'] = false; $stored_capabilities = sanitize_capabilities_payload_logic($metadata['capabilities'] ?? null); if (!empty($stored_capabilities)) { $resolved = array_merge($resolved, $stored_capabilities); } $input_modalities = normalize_capability_list_logic( $metadata['input_modalities'] ?? ($metadata['architecture']['input_modalities'] ?? []) ); if (!empty($input_modalities)) { $resolved['image_input'] = in_array('image', $input_modalities, true) || in_array('image_url', $input_modalities, true) || in_array('input_image', $input_modalities, true); } $output_modalities = normalize_capability_list_logic( $metadata['output_modalities'] ?? ($metadata['architecture']['output_modalities'] ?? []) ); if (!empty($output_modalities)) { $supports_image_output = in_array('image', $output_modalities, true) || in_array('image_url', $output_modalities, true) || in_array('output_image', $output_modalities, true); $resolved['image_output'] = $supports_image_output; $resolved['image_generation'] = $supports_image_output; } $supported_features = normalize_capability_list_logic( $metadata['supported_features'] ?? ($metadata['supportedFeatures'] ?? []) ); if (!empty($supported_features)) { $supports_tools_feature = in_array('tools', $supported_features, true) || in_array('tool_use', $supported_features, true) || in_array('function_calling', $supported_features, true) || in_array('function-calling', $supported_features, true) || in_array('plugins', $supported_features, true); $supports_web_feature = in_array('web_search', $supported_features, true) || in_array('web-search', $supported_features, true) || in_array('web', $supported_features, true) || $supports_tools_feature; $supports_image_generation_feature = in_array('image_generation', $supported_features, true) || in_array('image-generation', $supported_features, true); $resolved['tools'] = $supports_tools_feature; $resolved['web_search_plugin'] = $supports_web_feature; $resolved['image_generation'] = $supports_image_generation_feature || $resolved['image_generation']; $resolved['image_output'] = $resolved['image_generation']; if (in_array('embeddings', $supported_features, true) || in_array('embedding', $supported_features, true)) { $resolved['embeddings'] = true; } } $supported_parameters = normalize_capability_list_logic( $metadata['supported_parameters'] ?? ($metadata['supportedParameters'] ?? ($metadata['supported_sampling_parameters'] ?? [])) ); if (!empty($supported_parameters)) { $supports_tools_param = in_array('plugins', $supported_parameters, true) || in_array('tools', $supported_parameters, true) || in_array('tool_choice', $supported_parameters, true) || in_array('parallel_tool_calls', $supported_parameters, true); if (empty($supported_features)) { $resolved['tools'] = $supports_tools_param; $resolved['web_search_plugin'] = $supports_tools_param; } else { $resolved['tools'] = !empty($resolved['tools']) || $supports_tools_param; $resolved['web_search_plugin'] = !empty($resolved['web_search_plugin']) || $supports_tools_param; } } if (array_key_exists('pricing_web_search', $metadata)) { $resolved['web_search_plugin'] = true; } $supports_image_output_by_modality = in_array('image', $output_modalities, true) || in_array('image_url', $output_modalities, true) || in_array('output_image', $output_modalities, true); $supports_image_generation_by_feature = in_array('image_generation', $supported_features, true) || in_array('image-generation', $supported_features, true) || in_array('image_output', $supported_features, true) || in_array('image-output', $supported_features, true); $model_id_l = strtolower((string) ($metadata['id'] ?? '')); $model_name_l = strtolower((string) ($metadata['name'] ?? '')); $image_haystack = trim($model_id_l . ' ' . $model_name_l); $is_auto_router = $model_id_l === 'openrouter/auto' || $model_id_l === 'auto' || strpos($model_id_l, 'auto-router') !== false || strpos($model_name_l, 'auto router') !== false; $looks_like_image_model = strpos($image_haystack, 'image') !== false || strpos($image_haystack, 'gpt-image') !== false || strpos($image_haystack, 'flux') !== false || strpos($image_haystack, 'stable-diffusion') !== false || strpos($image_haystack, 'sdxl') !== false || strpos($image_haystack, 'riverflow') !== false || strpos($image_haystack, 'imagen') !== false || strpos($image_haystack, 'nano banana') !== false || strpos($image_haystack, 'nano-banana') !== false; $has_explicit_image_metadata = !empty($output_modalities) || !empty($supported_features); if ($is_auto_router) { $supports_image = true; $resolved['image_output'] = true; $resolved['image_generation'] = true; } elseif ($has_explicit_image_metadata) { $supports_image = $supports_image_output_by_modality || $supports_image_generation_by_feature; $resolved['image_output'] = $supports_image; $resolved['image_generation'] = $supports_image; } else { $supports_image = $looks_like_image_model; $resolved['image_output'] = $supports_image; $resolved['image_generation'] = $supports_image; } if ($resolved['image_output'] !== $resolved['image_generation']) { $resolved['image_output'] = $resolved['image_generation'] || $resolved['image_output']; $resolved['image_generation'] = $resolved['image_output']; } return $resolved; } function get_model_metadata_logic(string $model_id): ?array { $model_id = sanitize_text_field($model_id); if ($model_id === '') { return null; } $synced_models = get_option('aipkit_openrouter_model_list', []); if (!is_array($synced_models)) { return null; } foreach ($synced_models as $model) { if (!is_array($model)) { continue; } $candidate_id = isset($model['id']) ? sanitize_text_field((string) $model['id']) : ''; if ($candidate_id !== '' && $candidate_id === $model_id) { return $model; } } return null; } function resolve_model_capabilities_logic(string $model_id): array { $metadata = get_model_metadata_logic($model_id); if (!is_array($metadata)) { return get_capability_map_logic(); } return resolve_model_capabilities_from_metadata_logic($metadata); } function model_supports_web_search_plugin_logic(string $model_id): bool { $resolved = resolve_model_capabilities_logic($model_id); return !empty($resolved['web_search_plugin']); } function model_supports_image_output_logic(string $model_id): bool { $resolved = resolve_model_capabilities_logic($model_id); return !empty($resolved['image_output']); } function model_output_modalities_logic(string $model_id): array { $metadata = get_model_metadata_logic($model_id); if (!is_array($metadata)) { return []; } return normalize_capability_list_logic( $metadata['output_modalities'] ?? ($metadata['architecture']['output_modalities'] ?? []) ); } function model_supports_image_config_logic(string $model_id): bool { $model_id = strtolower(sanitize_text_field($model_id)); if ($model_id === '') { return false; } $metadata = get_model_metadata_logic($model_id); if (is_array($metadata)) { $supported_parameters = normalize_capability_list_logic( $metadata['supported_parameters'] ?? ($metadata['supportedParameters'] ?? ($metadata['supported_sampling_parameters'] ?? [])) ); foreach ($supported_parameters as $parameter) { if ( $parameter === 'image_config' || $parameter === 'image_config.aspect_ratio' || $parameter === 'image_config.image_size' ) { return true; } } } $is_google_image_model = strpos($model_id, 'google/gemini-') === 0 && strpos($model_id, 'image') !== false; $is_flux_model = strpos($model_id, 'black-forest-labs/flux') === 0; $is_recraft_model = strpos($model_id, 'recraft/') === 0; $is_sourceful_model = strpos($model_id, 'sourceful/riverflow') === 0; return $is_google_image_model || $is_flux_model || $is_recraft_model || $is_sourceful_model; } function model_supports_image_editing_logic(string $model_id): bool { $resolved = resolve_model_capabilities_logic($model_id); return !empty($resolved['image_input']) && !empty($resolved['image_output']); } function sanitize_web_search_config_logic(array $raw_config): array { $sanitized = []; if (isset($raw_config['max_results'])) { $max_results = absint($raw_config['max_results']); if ($max_results > 0) { $sanitized['max_results'] = max(1, min($max_results, 10)); } } if (!empty($raw_config['search_prompt'])) { $search_prompt = AIPKit_Prompt_Sanitizer::sanitize($raw_config['search_prompt']); if ($search_prompt !== '') { $sanitized['search_prompt'] = $search_prompt; } } if (!empty($raw_config['engine'])) { $engine = sanitize_key((string) $raw_config['engine']); if (in_array($engine, ['native', 'exa'], true)) { $sanitized['engine'] = $engine; } } return $sanitized; } 