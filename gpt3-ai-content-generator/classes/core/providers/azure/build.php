<?php
 namespace WPAICG\Core\Providers\Azure\Methods; use WP_Error; if (!defined('ABSPATH')) { exit; } function build_logic_for_url_builder(string $operation, array $params): string|WP_Error { $azure_endpoint = !empty($params['azure_endpoint']) ? rtrim($params['azure_endpoint'], '/') : ''; $deployment_name = !empty($params['deployment']) ? $params['deployment'] : ''; if (empty($azure_endpoint)) return new WP_Error('missing_azure_endpoint_logic', __('Azure endpoint is required.', 'gpt3-ai-content-generator')); $api_version = ''; if ($operation === 'deployments' || $operation === 'models') { $api_version = $params['azure_authoring_version'] ?? '2023-03-15-preview'; } else { $api_version = $params['azure_inference_version'] ?? '2024-02-01'; } if (empty($api_version)) return new WP_Error('missing_azure_api_version_logic', __('Azure API Version is required.', 'gpt3-ai-content-generator')); $paths = [ 'chat' => '/chat/completions', 'deployments' => '/openai/deployments', 'models' => '/openai/models', 'embeddings' => '/embeddings', ]; $path_key = ($operation === 'stream') ? 'chat' : $operation; $path_segment = $paths[$path_key] ?? null; if ($path_segment === null) { return new WP_Error('unsupported_operation_Azure_logic', sprintf(__('Operation "%s" not supported for Azure.', 'gpt3-ai-content-generator'), $operation)); } $query_param = '?api-version=' . urlencode($api_version); if ($operation === 'deployments' || $operation === 'models') { return $azure_endpoint . $path_segment . $query_param; } elseif ($operation === 'chat' || $operation === 'stream' || $operation === 'embeddings') { if (empty($deployment_name)) return new WP_Error('missing_azure_deployment_logic', __('Azure deployment name is required for this operation.', 'gpt3-ai-content-generator')); return $azure_endpoint . '/openai/deployments/' . urlencode($deployment_name) . $path_segment . $query_param; } else { return new WP_Error('unhandled_azure_operation_logic', sprintf(__('Unhandled Azure operation path building for: %s', 'gpt3-ai-content-generator'), $operation)); } }