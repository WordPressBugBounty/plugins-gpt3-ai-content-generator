<?php
 namespace WPAICG\Core\Stream\Vector; use WPAICG\Core\AIPKit_AI_Caller; use WPAICG\Vector\AIPKit_Vector_Store_Manager; use WPAICG\AIPKit_Providers; use WP_Error; $build_context_path = __DIR__ . '/build-context/'; require_once $build_context_path . 'check-prerequisites.php'; require_once $build_context_path . 'normalize-embedding-provider.php'; require_once $build_context_path . 'resolve-embedding-vector.php'; require_once $build_context_path . 'build-score-item.php'; require_once $build_context_path . 'resolve-openai-context.php'; require_once $build_context_path . 'resolve-pinecone-context.php'; require_once $build_context_path . 'resolve-qdrant-context.php'; require_once $build_context_path . 'resolve-chroma-context.php'; if (!defined('ABSPATH')) { exit; } function build_vector_search_context_logic( AIPKit_AI_Caller $ai_caller, AIPKit_Vector_Store_Manager $vector_store_manager, string $user_message, array $bot_settings, string $main_provider, ?string $frontend_active_openai_vs_id = null, ?string $frontend_active_pinecone_index_name = null, ?string $frontend_active_pinecone_namespace = null, ?string $frontend_active_qdrant_collection_name = null, ?string $frontend_active_qdrant_file_upload_context_id = null, ?string $frontend_active_chroma_collection_name = null, ?string $frontend_active_chroma_file_upload_context_id = null, ?array &$vector_search_scores_output = null ): string { global $wpdb; $data_source_table_name = $wpdb->prefix . 'aipkit_vector_data_source'; $vector_store_enabled = ($bot_settings['enable_vector_store'] ?? '0') === '1'; if (!BuildContext\check_prerequisites_logic($vector_store_enabled, $user_message, $ai_caller, $vector_store_manager)) { return ""; } $all_formatted_results = ""; $vector_provider_from_bot = $bot_settings['vector_store_provider'] ?? ''; $vector_top_k = absint($bot_settings['vector_store_top_k'] ?? 3); $vector_top_k = max(1, min($vector_top_k, 20)); if ($vector_search_scores_output !== null) { $vector_search_scores_output = []; } if ($vector_provider_from_bot === 'openai') { $openai_results = BuildContext\resolve_openai_context_logic( $vector_store_manager, $user_message, $bot_settings, $main_provider, $frontend_active_openai_vs_id, $vector_top_k, $vector_search_scores_output ); $all_formatted_results .= $openai_results; } elseif ($vector_provider_from_bot === 'pinecone') { $pinecone_results = BuildContext\resolve_pinecone_context_logic( $ai_caller, $vector_store_manager, $user_message, $bot_settings, $frontend_active_pinecone_index_name, $frontend_active_pinecone_namespace, $vector_top_k, $wpdb, $data_source_table_name, $vector_search_scores_output ); $all_formatted_results .= $pinecone_results; } elseif ($vector_provider_from_bot === 'qdrant') { $qdrant_results = BuildContext\resolve_qdrant_context_logic( $ai_caller, $vector_store_manager, $user_message, $bot_settings, $frontend_active_qdrant_collection_name, $frontend_active_qdrant_file_upload_context_id, $vector_top_k, $wpdb, $data_source_table_name, $vector_search_scores_output ); $all_formatted_results .= $qdrant_results; } elseif ($vector_provider_from_bot === 'chroma') { $chroma_results = BuildContext\resolve_chroma_context_logic( $ai_caller, $vector_store_manager, $user_message, $bot_settings, $frontend_active_chroma_collection_name, $frontend_active_chroma_file_upload_context_id, $vector_top_k, $wpdb, $data_source_table_name, $vector_search_scores_output ); $all_formatted_results .= $chroma_results; } return trim($all_formatted_results); } 