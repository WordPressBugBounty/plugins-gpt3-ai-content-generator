<?php
 namespace WPAICG\AIForms\Storage\Methods; use WPAICG\AIForms\Admin\AIPKit_AI_Form_Admin_Setup; use WP_Query; if (!defined('ABSPATH')) { exit; } function get_forms_list_logic(\WPAICG\AIForms\Storage\AIPKit_AI_Form_Storage $storageInstance, array $args = []): array { if (!class_exists(AIPKit_AI_Form_Admin_Setup::class)) { return ['forms' => [], 'pagination' => ['total_forms' => 0, 'total_pages' => 0]]; } $defaults = [ 'post_type' => AIPKit_AI_Form_Admin_Setup::POST_TYPE, 'post_status' => ['publish', 'draft'], 'posts_per_page' => 20, 'paged' => 1, 's' => '', 'orderby' => 'modified', 'order' => 'DESC', ]; if (isset($args['search'])) { $args['s'] = $args['search']; unset($args['search']); } $query_args = wp_parse_args($args, $defaults); $meta_query = []; if (!empty($args['filter_provider']) && $args['filter_provider'] !== 'all') { $meta_query[] = [ 'key' => '_aipkit_ai_form_ai_provider', 'value' => $args['filter_provider'], 'compare' => '=', ]; } if (isset($args['orderby']) && in_array($args['orderby'], ['provider', 'model'])) { $sort_key = '_aipkit_ai_form_ai_' . $args['orderby']; $query_args['meta_key'] = $sort_key; $query_args['orderby'] = 'meta_value'; } if (!empty($meta_query)) { if (count($meta_query) > 1) { $meta_query['relation'] = 'AND'; } $query_args['meta_query'] = $meta_query; } $query = new WP_Query($query_args); $forms_data = []; $post_ids = []; if ($query->have_posts()) { $post_ids = wp_list_pluck($query->posts, 'ID'); } $meta_map = []; if (!empty($post_ids)) { global $wpdb; $cache_key = 'aipkit_ai_forms_list_meta_' . md5(implode(',', $post_ids)); $cache_group = 'aipkit_ai_forms'; $all_meta_results = wp_cache_get($cache_key, $cache_group); if (false === $all_meta_results) { $id_placeholders = implode(', ', array_fill(0, count($post_ids), '%d')); $meta_keys_to_fetch = [ '_aipkit_ai_form_ai_provider', '_aipkit_ai_form_ai_model', '_aipkit_ai_form_submission_count', ]; $meta_key_placeholders = implode(', ', array_fill(0, count($meta_keys_to_fetch), '%s')); $meta_query_sql = $wpdb->prepare("SELECT post_id, meta_key, meta_value FROM {$wpdb->postmeta} WHERE post_id IN ($id_placeholders) AND meta_key IN ($meta_key_placeholders)", array_merge($post_ids, $meta_keys_to_fetch)); $all_meta_results = $wpdb->get_results($meta_query_sql, ARRAY_A); wp_cache_set($cache_key, $all_meta_results, $cache_group, MINUTE_IN_SECONDS); } if (is_array($all_meta_results)) { foreach ($all_meta_results as $meta_row) { $meta_map[$meta_row['post_id']][$meta_row['meta_key']] = $meta_row['meta_value']; } } } if ($query->have_posts()) { foreach ($query->posts as $post) { $form_id = $post->ID; $updated_at = $post->post_modified_gmt; if (!$updated_at || '0000-00-00 00:00:00' === $updated_at) { $updated_at = $post->post_date_gmt; } $submission_count_raw = $meta_map[$form_id]['_aipkit_ai_form_submission_count'] ?? 0; $submission_count = is_numeric($submission_count_raw) ? (int) $submission_count_raw : 0; $forms_data[] = [ 'id' => $form_id, 'title' => $post->post_title, 'shortcode' => '[aipkit_ai_form id=' . $form_id . ']', 'status' => $post->post_status, 'provider' => $meta_map[$form_id]['_aipkit_ai_form_ai_provider'] ?? null, 'model' => $meta_map[$form_id]['_aipkit_ai_form_ai_model'] ?? null, 'updated_at' => $updated_at, 'submissions_count' => $submission_count, ]; } wp_reset_postdata(); } $forms_data = apply_filters('aipkit_ai_forms_list_forms_data', $forms_data, $post_ids, $storageInstance); return [ 'forms' => $forms_data, 'pagination' => [ 'total_forms' => (int) $query->found_posts, 'total_pages' => (int) $query->max_num_pages, ] ]; } 