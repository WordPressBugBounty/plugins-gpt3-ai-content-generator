<?php
 namespace WPAICG\Chat\Frontend\Shortcode\FeatureManagerMethods; use WPAICG\AIPKit_Providers; use WPAICG\aipkit_dashboard; use function WPAICG\Core\Providers\OpenRouter\Methods\resolve_model_capabilities_logic; if (!defined('ABSPATH')) { exit; } function get_upload_flags_logic(array $core_flags): array { $upload_flags = []; $is_pro = false; if (!class_exists(aipkit_dashboard::class)) { $dashboard_path = WPAICG_PLUGIN_DIR . 'classes/dashboard/class-aipkit_dashboard.php'; if (file_exists($dashboard_path)) { require_once $dashboard_path; } } if (class_exists(aipkit_dashboard::class)) { $is_pro = aipkit_dashboard::is_pro_plan(); } $provider = isset($core_flags['provider']) ? sanitize_text_field((string) $core_flags['provider']) : 'OpenAI'; $model = isset($core_flags['model']) ? sanitize_text_field((string) $core_flags['model']) : ''; $vector_store_provider = isset($core_flags['vector_store_provider']) ? sanitize_key((string) $core_flags['vector_store_provider']) : 'openai'; $default_image_upload_supported_providers = ['OpenAI', 'Claude', 'OpenRouter', 'xAI']; $image_upload_supported_providers = apply_filters( 'aipkit_chat_image_upload_supported_providers', $default_image_upload_supported_providers, $core_flags, $is_pro ); if (!is_array($image_upload_supported_providers)) { $image_upload_supported_providers = $default_image_upload_supported_providers; } else { $image_upload_supported_providers = array_values(array_filter(array_map( static fn($item) => is_string($item) ? sanitize_text_field($item) : '', $image_upload_supported_providers ))); if (empty($image_upload_supported_providers)) { $image_upload_supported_providers = $default_image_upload_supported_providers; } } $is_image_upload_supported_provider = in_array($provider, $image_upload_supported_providers, true); $claude_files_compatible = !($vector_store_provider === 'claude_files' && $provider !== 'Claude'); if ($provider === 'OpenRouter' && $is_image_upload_supported_provider && $model !== '') { $resolver_fn = 'WPAICG\\Core\\Providers\\OpenRouter\\Methods\\resolve_model_capabilities_logic'; if (!function_exists($resolver_fn)) { $capability_file = WPAICG_PLUGIN_DIR . 'classes/core/providers/openrouter/capabilities.php'; if (file_exists($capability_file)) { require_once $capability_file; } } if (function_exists($resolver_fn)) { $capabilities = resolve_model_capabilities_logic($model); $is_image_upload_supported_provider = !empty($capabilities['image_input']); } } if ($provider === 'xAI' && $is_image_upload_supported_provider && $model !== '') { if (!class_exists(AIPKit_Providers::class)) { $providers_path = WPAICG_PLUGIN_DIR . 'classes/dashboard/class-aipkit_providers.php'; if (file_exists($providers_path)) { require_once $providers_path; } } if (class_exists(AIPKit_Providers::class)) { $is_image_upload_supported_provider = AIPKit_Providers::xai_model_supports_image_input($model); } } $is_image_upload_supported_provider = (bool) apply_filters( 'aipkit_chat_image_upload_model_supported', $is_image_upload_supported_provider, [ 'provider' => $provider, 'model' => $model, 'core_flags' => $core_flags, 'is_pro_plan' => $is_pro, 'vector_store_provider' => $vector_store_provider, ] ); $upload_flags['file_upload_ui_enabled'] = ($core_flags['enable_file_upload_setting'] ?? false) && $is_pro && $claude_files_compatible; $upload_flags['image_upload_ui_enabled'] = ($core_flags['enable_image_upload_setting'] ?? false) && $is_image_upload_supported_provider; $upload_flags['input_action_button_enabled'] = $upload_flags['file_upload_ui_enabled'] || $upload_flags['image_upload_ui_enabled']; return $upload_flags; } 