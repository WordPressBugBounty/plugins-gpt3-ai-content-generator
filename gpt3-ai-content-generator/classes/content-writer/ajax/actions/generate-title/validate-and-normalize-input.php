<?php
 namespace WPAICG\ContentWriter\Ajax\Actions\GenerateTitle; use WPAICG\ContentWriter\Ajax\Actions\AIPKit_Content_Writer_Generate_Title_Action; use WPAICG\AIPKit_Providers; use WPAICG\Utils\AIPKit_Prompt_Sanitizer; use WP_Error; if (!defined('ABSPATH')) { exit; } function validate_and_normalize_input_logic(AIPKit_Content_Writer_Generate_Title_Action $handler): array|WP_Error { $permission_check = $handler->check_module_access_permissions('content-writer', 'aipkit_content_writer_nonce'); if (is_wp_error($permission_check)) { return $permission_check; } if (!$handler->get_ai_caller()) { return new WP_Error('ai_caller_missing', __('AI processing component is unavailable.', 'gpt3-ai-content-generator'), ['status' => 500]); } $settings = isset($_POST) ? wp_unslash($_POST) : []; $original_title = isset($settings['content_title']) ? sanitize_text_field(wp_unslash($settings['content_title'])) : ''; if (empty($original_title)) { return new WP_Error('missing_original_title', __('Original title/topic is required to generate a new title.', 'gpt3-ai-content-generator'), ['status' => 400]); } $topic = $original_title; $inline_keywords = ''; if (strpos($original_title, '|') !== false) { $parts = explode('|', $original_title, 2); $topic = trim($parts[0]); $inline_keywords = isset($parts[1]) ? trim($parts[1]) : ''; } $provider_raw = isset($settings['ai_provider']) && !empty($settings['ai_provider']) ? sanitize_text_field($settings['ai_provider']) : AIPKit_Providers::get_current_provider(); $provider = AIPKit_Providers::normalize_provider_label($provider_raw); $model_data = AIPKit_Providers::get_provider_data($provider); $model = isset($settings['ai_model']) && !empty($settings['ai_model']) ? sanitize_text_field($settings['ai_model']) : ($model_data['model'] ?? ''); if (empty($model)) { return new WP_Error('missing_model_title_gen', __('AI model selection is required for title generation.', 'gpt3-ai-content-generator'), ['status' => 400]); } $settings['ai_temperature'] = isset($settings['ai_temperature']) ? floatval($settings['ai_temperature']) : 1.0; $settings['custom_title_prompt'] = isset($settings['custom_title_prompt']) ? AIPKit_Prompt_Sanitizer::sanitize($settings['custom_title_prompt']) : ''; $settings['rss_description'] = isset($settings['rss_description']) ? sanitize_textarea_field(wp_unslash($settings['rss_description'])) : ''; $settings['url_content_context'] = isset($settings['url_content_context']) ? sanitize_textarea_field(wp_unslash($settings['url_content_context'])) : ''; $settings['source_url'] = isset($settings['source_url']) ? esc_url_raw(wp_unslash($settings['source_url'])) : ''; $validated_params = $settings; $validated_params['content_title'] = $topic; $validated_params['inline_keywords'] = $inline_keywords; $validated_params['provider'] = $provider; $validated_params['model'] = $model; return $validated_params; } 