<?php
 namespace WPAICG\ContentWriter\Ajax\Actions; use WPAICG\ContentWriter\Ajax\AIPKit_Content_Writer_Base_Ajax_Action; use WP_Error; $shared_path = __DIR__ . '/shared/'; require_once $shared_path . 'methods.php'; $init_stream_path = __DIR__ . '/init-stream/'; require_once $init_stream_path . 'methods.php'; if (!defined('ABSPATH')) { exit; } class AIPKit_Content_Writer_Init_Stream_Action extends AIPKit_Content_Writer_Base_Ajax_Action { public function handle() { $settings = isset($_POST) ? wp_unslash($_POST) : []; $validated_params = Shared\validate_and_normalize_input_logic($this, $settings); if (is_wp_error($validated_params)) { $this->send_wp_error($validated_params); return; } $cache_check_result = InitStream\ensure_sse_cache_available_logic(); if (is_wp_error($cache_check_result)) { $this->send_wp_error($cache_check_result); return; } $keyword_resolution = []; $resolved_keyword_params = Shared\resolve_smart_seo_keywords_logic( $validated_params, $this->get_ai_caller(), [ 'topic' => $validated_params['content_title'] ?? '', 'title' => $validated_params['content_title'] ?? '', ] ); $validated_params = $resolved_keyword_params['params']; $keyword_resolution = $resolved_keyword_params['resolution']; $prompts = Shared\build_prompts_logic($validated_params); if (is_wp_error($prompts)) { $this->send_wp_error($prompts); return; } $ai_params_for_cache = InitStream\merge_ai_params_logic($validated_params); $data_to_cache = InitStream\build_cache_payload_logic( $prompts['system_instruction'], $prompts['user_prompt'], $validated_params['provider'], $validated_params['model'], $ai_params_for_cache, $validated_params ); $cache_key_result = InitStream\write_to_sse_cache_logic($data_to_cache); if (is_wp_error($cache_key_result)) { $this->send_wp_error($cache_key_result); return; } $conversation_uuid = isset($data_to_cache['conversation_uuid']) ? $data_to_cache['conversation_uuid'] : ''; wp_send_json_success(array_merge( ['cache_key' => $cache_key_result, 'conversation_uuid' => $conversation_uuid], Shared\smart_seo_keyword_resolution_response_fields_logic($keyword_resolution) )); } } 