<?php
 namespace WPAICG\STT; use WP_Error; use WPAICG\AIPKit_Providers; use WPAICG\Chat\Storage\BotStorage; use WPAICG\Utils\AIPKit_CORS_Manager; if (!defined('ABSPATH')) { exit; } class AIPKit_STT_Manager { private $bot_storage; public function __construct() { if (!class_exists(\WPAICG\Chat\Storage\BotStorage::class)) { return; } $this->bot_storage = new BotStorage(); } public function init_hooks() { add_action('wp_ajax_aipkit_transcribe_audio', [$this, 'ajax_transcribe_audio']); add_action('wp_ajax_nopriv_aipkit_transcribe_audio', [$this, 'ajax_transcribe_audio']); } public function speech_to_text(string $audio_data, string $audio_format, array $options = []): string|WP_Error { $stt_provider = null; $bot_settings = []; if (!empty($options['bot_id'])) { $bot_settings = $this->bot_storage->get_chatbot_settings(absint($options['bot_id'])); $stt_provider = $bot_settings['stt_provider'] ?? null; if (($stt_provider === 'OpenAI' || $options['provider'] === 'OpenAI') && isset($bot_settings['stt_openai_model_id'])) { $options['stt_model'] = $bot_settings['stt_openai_model_id']; } if (($stt_provider === 'Azure' || $options['provider'] === 'Azure') && isset($bot_settings['stt_azure_model_id'])) { $options['stt_model'] = $bot_settings['stt_azure_model_id']; } } $provider = $stt_provider ?: ($options['provider'] ?? AIPKit_Providers::get_current_provider()); $valid_stt_providers = ['OpenAI', 'Azure']; if (!in_array($provider, $valid_stt_providers)) { $provider = 'OpenAI'; } $strategy = AIPKit_STT_Provider_Strategy_Factory::get_strategy($provider); if (is_wp_error($strategy)) { return $strategy; } $provider_data = AIPKit_Providers::get_provider_data($provider); $api_params = [ 'api_key' => $provider_data['api_key'] ?? null, 'base_url' => $provider_data['base_url'] ?? null, 'azure_endpoint' => $provider_data['endpoint'] ?? null, 'stt_model' => $options['stt_model'] ?? null ]; if (empty($api_params['api_key'])) { return new WP_Error('missing_stt_api_key', sprintf(__('API Key for STT provider %s is missing.', 'gpt3-ai-content-generator'), $provider)); } if ($provider === 'Azure' && empty($api_params['azure_endpoint'])) { return new WP_Error('missing_stt_endpoint', __('Azure Endpoint/Region URL is required for STT.', 'gpt3-ai-content-generator')); } $supported_formats = $strategy->get_supported_formats(); if (!in_array(strtolower($audio_format), $supported_formats)) { return new WP_Error('unsupported_stt_format', sprintf(__('Audio format "%1$s" is not supported by %2$s STT.', 'gpt3-ai-content-generator'), $audio_format, $provider)); } $result = $strategy->transcribe_audio($audio_data, $audio_format, $api_params, $options); return $result; } public function ajax_transcribe_audio() { AIPKit_CORS_Manager::handle_preflight_request(); $post_data = wp_unslash($_POST); $bot_id = isset($post_data['bot_id']) ? absint($post_data['bot_id']) : 0; if ($bot_id > 0) { $is_cross_origin = false; if (isset($_SERVER['HTTP_ORIGIN']) && !empty($_SERVER['HTTP_ORIGIN'])) { $origin = esc_url_raw(wp_unslash((string) $_SERVER['HTTP_ORIGIN'])); $site_url = get_site_url(); $site_parsed = wp_parse_url($site_url); $origin_parsed = wp_parse_url($origin); if ( $origin_parsed && $site_parsed && ( ($origin_parsed['host'] ?? '') !== ($site_parsed['host'] ?? '') || ($origin_parsed['scheme'] ?? 'http') !== ($site_parsed['scheme'] ?? 'http') ) ) { $is_cross_origin = true; } } if ($is_cross_origin) { if (class_exists('\WPAICG\aipkit_dashboard') && \WPAICG\aipkit_dashboard::is_pro_plan()) { $origin_allowed = AIPKit_CORS_Manager::check_and_set_cors_headers($bot_id); if (!$origin_allowed) { wp_send_json_error([ 'message' => __('This domain is not permitted to access the chatbot.', 'gpt3-ai-content-generator'), 'code' => 'cors_denied', ], 403); return; } } else { wp_send_json_error([ 'message' => __('Embed feature is not available with your current plan.', 'gpt3-ai-content-generator'), 'code' => 'embed_not_available', ], 403); return; } } } if (!check_ajax_referer('aipkit_frontend_chat_nonce', '_ajax_nonce', false)) { wp_send_json_error(['message' => __('Security check failed (nonce).', 'gpt3-ai-content-generator')], 403); return; } if (empty($bot_id)) { wp_send_json_error(['message' => __('Bot ID is required for transcription.', 'gpt3-ai-content-generator')], 400); return; } $options = ['bot_id' => $bot_id]; if (isset($post_data['language'])) { $options['language'] = sanitize_text_field((string) $post_data['language']); } $audio_data_binary = ''; $audio_format = 'webm'; $audio_file = (isset($_FILES['audio_file']) && is_array($_FILES['audio_file'])) ? $_FILES['audio_file'] : null; if ( is_array($audio_file) && isset($audio_file['tmp_name']) && is_string($audio_file['tmp_name']) && $audio_file['tmp_name'] !== '' && is_uploaded_file($audio_file['tmp_name']) ) { $tmp_name = $audio_file['tmp_name']; $file_size = isset($audio_file['size']) ? (int) $audio_file['size'] : 0; $original_name = sanitize_file_name((string) ($audio_file['name'] ?? 'audio')); $max_size = (int) apply_filters('aipkit_stt_max_audio_bytes', 4 * 1024 * 1024); if ($file_size <= 0) { wp_send_json_error(['message' => __('Uploaded audio file is empty.', 'gpt3-ai-content-generator')], 400); return; } if ($file_size > $max_size) { wp_send_json_error(['message' => sprintf(__('Audio file too large. Max size: %d bytes.', 'gpt3-ai-content-generator'), $max_size)], 413); return; } $mime = function_exists('mime_content_type') ? mime_content_type($tmp_name) : ((isset($audio_file['type']) && is_string($audio_file['type'])) ? $audio_file['type'] : ''); $allowed_mime_map = [ 'audio/webm' => 'webm', 'audio/wav' => 'wav', 'audio/x-wav' => 'wav', 'audio/mpeg' => 'mp3', 'audio/mp3' => 'mp3', 'audio/ogg' => 'ogg', 'audio/ogg; codecs=opus' => 'ogg', ]; if (isset($allowed_mime_map[$mime])) { $audio_format = $allowed_mime_map[$mime]; } else { $ext = strtolower(pathinfo($original_name, PATHINFO_EXTENSION)); if (in_array($ext, ['webm', 'wav', 'mp3', 'ogg'])) { $audio_format = $ext; } else { wp_send_json_error(['message' => __('Unsupported audio MIME type.', 'gpt3-ai-content-generator')], 400); return; } } $audio_data_binary = file_get_contents($tmp_name); if ($audio_data_binary === false || $audio_data_binary === '') { wp_send_json_error(['message' => __('Failed to read uploaded audio file.', 'gpt3-ai-content-generator')], 400); return; } } else { $audio_base64 = isset($post_data['audio_data']) ? (string) $post_data['audio_data'] : ''; if (strpos($audio_base64, 'base64,') !== false) { $audio_base64 = substr($audio_base64, strpos($audio_base64, 'base64,') + 7); } $audio_data_binary = base64_decode($audio_base64, true); $audio_format = isset($post_data['audio_format']) ? sanitize_text_field((string) $post_data['audio_format']) : 'webm'; if (empty($audio_data_binary)) { wp_send_json_error(['message' => __('Invalid or empty audio data received.', 'gpt3-ai-content-generator')], 400); return; } $max_size = (int) apply_filters('aipkit_stt_max_audio_bytes', 4 * 1024 * 1024); if (strlen($audio_data_binary) > $max_size) { wp_send_json_error(['message' => sprintf(__('Audio data too large after decoding. Max size: %d bytes.', 'gpt3-ai-content-generator'), $max_size)], 413); return; } } $transcription_result = $this->speech_to_text($audio_data_binary, $audio_format, $options); if (is_wp_error($transcription_result)) { $error_data = $transcription_result->get_error_data(); $status_code = isset($error_data['status']) ? (int)$error_data['status'] : 500; wp_send_json_error(['message' => $transcription_result->get_error_message()], $status_code); } else { wp_send_json_success(['transcription' => $transcription_result]); } } } 