<?php
 namespace WPAICG\Images\Providers\Google; use WP_Error; if (!defined('ABSPATH')) { exit; } class GoogleVideoResponseParser { const VEO_TOKENS_PER_VIDEO = 3000; const MAX_POLL_ATTEMPTS = 60; const POLL_INTERVAL = 10; public static function parse(array $decoded_response, string $model_id, array $api_params): array|WP_Error { $operation_name = $decoded_response['name'] ?? null; if (empty($operation_name)) { return new WP_Error('no_operation_name', __('No operation name found in Google Video API response.', 'gpt3-ai-content-generator')); } return [ 'status' => 'processing', 'operation_name' => $operation_name, 'model_id' => $model_id, 'api_params' => $api_params, 'message' => __('Video generation started. Please wait...', 'gpt3-ai-content-generator') ]; } public static function check_operation_status(string $operation_name, string $model_id, array $api_params, string $prompt = '', ?int $user_id = null): array|WP_Error { $poll_result = self::poll_operation($operation_name, $api_params); if (is_wp_error($poll_result)) { return $poll_result; } if (isset($poll_result['done']) && $poll_result['done'] === true) { if (isset($poll_result['response']['generateVideoResponse']['generatedSamples'])) { $samples = $poll_result['response']['generateVideoResponse']['generatedSamples']; $videos = []; if (is_array($samples) && !empty($samples)) { foreach ($samples as $index => $sample) { if (isset($sample['video']['uri'])) { $video_url = self::download_video($sample['video']['uri'], $api_params, $prompt, $user_id, $model_id); if (!is_wp_error($video_url)) { $videos[] = ['url' => $video_url, 'type' => 'video']; } else { return $video_url; } } } } else { return new WP_Error('no_video_samples', __('No video samples found in API response.', 'gpt3-ai-content-generator')); } $usage = null; $num_videos_generated = count($videos); if ($num_videos_generated > 0) { $total_tokens_for_video = $num_videos_generated * self::VEO_TOKENS_PER_VIDEO; $usage = [ 'input_tokens' => 0, 'output_tokens' => $total_tokens_for_video, 'total_tokens' => $total_tokens_for_video, 'provider_raw' => [ 'source' => 'hardcoded_veo_cost', 'videos_generated' => $num_videos_generated, 'cost_per_video_tokens' => self::VEO_TOKENS_PER_VIDEO, ], ]; } return ['videos' => $videos, 'usage' => $usage, 'status' => 'completed']; } else { return new WP_Error('no_video_in_response', __('No video data found in completed Google Video API response.', 'gpt3-ai-content-generator')); } } if (isset($poll_result['error'])) { $error_message = $poll_result['error']['message'] ?? __('Unknown error occurred during video generation.', 'gpt3-ai-content-generator'); return new WP_Error('google_video_generation_error', $error_message); } return [ 'status' => 'processing', 'operation_name' => $operation_name, 'message' => __('Video generation in progress...', 'gpt3-ai-content-generator') ]; } private static function poll_operation(string $operation_name, array $api_params): array|WP_Error { $poll_url = GoogleVideoUrlBuilder::build($operation_name, $api_params, 'poll'); if (is_wp_error($poll_url)) { return $poll_url; } $response = wp_remote_get($poll_url, [ 'timeout' => 30, 'headers' => [ 'Content-Type' => 'application/json', ], ]); if (is_wp_error($response)) { return new WP_Error('poll_http_error', __('HTTP error during Google Video API polling.', 'gpt3-ai-content-generator')); } $status_code = wp_remote_retrieve_response_code($response); $body = wp_remote_retrieve_body($response); $decoded_response = json_decode($body, true); if ($status_code !== 200 || empty($decoded_response)) { return new WP_Error('poll_api_error', sprintf(__('Google Video API polling error (%d).', 'gpt3-ai-content-generator'), $status_code)); } return $decoded_response; } private static function download_video(string $video_uri, array $api_params, string $prompt = '', ?int $user_id = null, string $model_id = 'veo-3.0-generate-preview'): string|WP_Error { $api_key = $api_params['api_key'] ?? ''; $response = wp_remote_get($video_uri, [ 'timeout' => 120, 'headers' => [ 'x-goog-api-key' => $api_key, ], ]); if (is_wp_error($response)) { return new WP_Error('video_download_error', __('Failed to download video from Google API.', 'gpt3-ai-content-generator')); } $status_code = wp_remote_retrieve_response_code($response); if ($status_code !== 200) { return new WP_Error('video_download_http_error', sprintf(__('Video download failed with status %d.', 'gpt3-ai-content-generator'), $status_code)); } $video_data = wp_remote_retrieve_body($response); $video_size = strlen($video_data); if (empty($video_data)) { return new WP_Error('empty_video_data', __('Downloaded video data is empty.', 'gpt3-ai-content-generator')); } $memory_limit = wp_convert_hr_to_bytes(ini_get('memory_limit')); $memory_usage = memory_get_usage(true); $available_memory = $memory_limit - $memory_usage; if ($video_size > $available_memory * 0.8) { return new WP_Error('insufficient_memory', __('Insufficient memory to process video file.', 'gpt3-ai-content-generator')); } $upload_dir = wp_upload_dir(); if (!empty($upload_dir['error'])) { return new WP_Error('upload_dir_error', __('Upload directory error: ', 'gpt3-ai-content-generator') . $upload_dir['error']); } if (!function_exists('WP_Filesystem')) { require_once ABSPATH . 'wp-admin/includes/file.php'; } $filesystem_ready = WP_Filesystem(); global $wp_filesystem; if (!$filesystem_ready || !$wp_filesystem) { return new WP_Error('filesystem_init_failed', __('Could not initialize filesystem.', 'gpt3-ai-content-generator')); } if (!$wp_filesystem->is_writable($upload_dir['path'])) { return new WP_Error('upload_dir_not_writable', __('Upload directory is not writable.', 'gpt3-ai-content-generator')); } $filename = 'veo3_video_' . time() . '_' . wp_generate_password(8, false) . '.mp4'; $file_path = trailingslashit($upload_dir['path']) . $filename; $result = $wp_filesystem->put_contents($file_path, $video_data, FS_CHMOD_FILE); if ($result === false) { return new WP_Error('video_save_error', __('Failed to save video file to uploads directory.', 'gpt3-ai-content-generator')); } $attachment_title = !empty($prompt) ? substr($prompt, 0, 100) : 'Veo 3 Generated Video'; $attachment = [ 'guid' => $upload_dir['url'] . '/' . $filename, 'post_mime_type' => 'video/mp4', 'post_title' => $attachment_title, 'post_content' => $prompt ?: '', 'post_status' => 'inherit' ]; if ($user_id && $user_id > 0) { $attachment['post_author'] = $user_id; } $attachment_id = wp_insert_attachment($attachment, $file_path); if (is_wp_error($attachment_id)) { return $attachment_id; } require_once(ABSPATH . 'wp-admin/includes/image.php'); try { $attachment_data = wp_generate_attachment_metadata($attachment_id, $file_path); $metadata_result = wp_update_attachment_metadata($attachment_id, $attachment_data); } catch (Exception $e) { } $meta1 = add_post_meta($attachment_id, '_aipkit_generated_video', '1'); $meta2 = add_post_meta($attachment_id, '_aipkit_video_model', $model_id); $meta3 = add_post_meta($attachment_id, '_aipkit_video_provider', 'Google'); $meta4 = $meta5 = $meta6 = true; if (!empty($prompt)) { $meta4 = add_post_meta($attachment_id, '_aipkit_video_prompt', $prompt); } if (isset($attachment_data['width']) && isset($attachment_data['height'])) { $size_string = $attachment_data['width'] . 'x' . $attachment_data['height']; $meta5 = add_post_meta($attachment_id, '_aipkit_video_size', $size_string); } if (isset($attachment_data['length'])) { $meta6 = add_post_meta($attachment_id, '_aipkit_video_duration', $attachment_data['length']); } $final_url = wp_get_attachment_url($attachment_id); return $final_url; } public static function parse_error(string $response_body, int $status_code): string { $decoded = json_decode($response_body, true); if (isset($decoded['error']['message'])) { return $decoded['error']['message']; } return sprintf(__('Google Video API returned status %d', 'gpt3-ai-content-generator'), $status_code); } } 